apiVersion: v1
kind: Service
metadata:
  name: data-fetcher-loadbalancer
  namespace: stock-data-fetcher
  labels:
    app: data-fetcher
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: data-fetcher
