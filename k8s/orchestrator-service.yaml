apiVersion: v1
kind: Service
metadata:
  name: orchestrator-service
  namespace: stock-data-fetcher
  labels:
    app: orchestrator
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: orchestrator
